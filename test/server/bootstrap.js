var request = require('supertest'),
  should = require('should'),
  sinon = require('sinon'),
  app = require('../../lib/app'),
  handlebars = require('../../lib/handlebars');

describe('handlebars', function() {
/*
  describe('when binding to an app', function() {
    it('should set the app view engine to handlebars', function() {
      var mock = sinon.mock(app);

      mock.expects('engine').withArgs('handlebars', sinon.match.typeOf('function'));

      handlebars.bind(app);

      mock.verify();
      mock.restore();
    });
  });

  describe('the app', function() {
    it('should have the engine handlebars configured', function() {
      app.get('view engine').should.equal('handlebars');
    });
  });
*/
});
